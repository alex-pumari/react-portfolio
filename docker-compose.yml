services:
  frontend:
    container_name: react-portfolio-frontend
    build: .
    ports:
      - "5173:5173"
      - "6006:6006"
    volumes:
      - .:/my-app
      - /my-app/node_modules
    command: npm run dev:container
